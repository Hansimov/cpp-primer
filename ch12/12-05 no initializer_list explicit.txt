我们未编写接受一个 initializer_list explicit（参见 7.5.4 节，第 264 页）参数的构造函数。讨论这个设计策略的优点和缺点。

关键字 explicit 可以避免将 initializer_list 自动转换成 StrBob。
这一设计易于使用，但难于调试。

优点：
    在出现自动转换时，编译器不会调用该构造函数。
    可以清晰地表达设计者的意图。
缺点：
    我们通常使用构造函数来创建一个只是临时的 StrBob 对象。
    不能用 explicit 的构造函数来实现 copy 形式的初始化，不方便使用。